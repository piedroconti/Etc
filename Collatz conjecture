#include <iostream>
using namespace std;

int main(){
  long long num;

  cin >> num;

  while(num != 1){
    if (num % 2 != 0){
      num = 3 * num + 1;
      cout << num << '\n';
    }
    else{
      num = num / 2;
      cout << num << '\n';
      }
  }

  return 0;
}
